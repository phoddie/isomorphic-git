/* eslint-disable no-debugger */
/* global trace */

// TODO: either write a git-apply-delta replacement in ES6 (like we've always wanted to do eventually)
// or patch 'git-apply-delta' and its dependencies: 'bops' and 'varint/decode.js'
export default function() {
  trace('TODO')
  debugger
}
